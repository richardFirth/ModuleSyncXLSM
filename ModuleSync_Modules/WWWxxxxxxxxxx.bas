Attribute VB_Name = "WWWxxxxxxxxxx"
Option Explicit

' modules below this point are syncable
' they are not specific to this macro

' (make a copy of the module syncer, and then use the copy to sync the modules)

' to do

' make dates automatically update

' done

' update print function to display code detect changes
' update bestmodules to be a global var vs hardcoded

' fix getHeader to be universal
' get it so that functions can automatically bump versions
